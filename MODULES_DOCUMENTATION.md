# VictorModules Documentation

This document provides details for all loaded VictorModules in Comfy Cortex.
It is auto-generated by `python generate_docs.py`.

---

## Bando Cognition Pipeline Core

-   **Node Name (Key)**: `BandoCognitionPipelineNode`
-   **File**: `victor_bando_cognition_pipeline.py`
-   **Version**: `v1.0.1-GODCORE-FUSION-NODE-REFACTORED`
-   **Category**: `VictorModules/Pipelines`
-   **Hash**: `(dynamic_hash_would_be_here)`
-   **Description (from metadata)**: Core node for running the Bando AGI Cognition Pipeline. Manages focus, directives, telemetry, and meta-loop processes. Uses imported core logic from 'bando_pipeline_core'.

### Class Docstring:
```
VictorModule Wrapper for BandoCognitionPipelineCore.
This node instantiates and runs the main cognitive pipeline.
It takes JSON strings for input data, context, and initial/rehydration state.
Outputs include a summary of the run and the updated pipeline state as JSON.
```

### Inputs:
    *   **Required Inputs:**
        *   `input_data_json` (`STRING`): Default = `{"prompt": "Hello Bando!"}`. Main input data for the pipeline (JSON string).
    *   **Optional Inputs:**
        *   `context_json` (`STRING`): Default = `{}`. Contextual information for the pipeline run (JSON string).
        *   `directive_str` (`STRING`): Default = ``. Initial directive (e.g., 'expand', 'reflect'). Empty for default.
        *   `pipeline_state_in_json` (`STRING`): Default = ``. JSON state from a previous run to rehydrate the pipeline.
        *   `initial_modes_json_list` (`STRING`): Default = `["reflect", "defend", "repair", "expand", "dream"]`. JSON list of modes for the directive router.

### Outputs:
    *   **Outputs:**
        *   `output_summary_json` (`STRING`)
        *   `pipeline_state_out_json` (`STRING`)

### Main Function (`execute_pipeline`):
```
Handles the execution of the BandoCognitionPipelineCore.
It initializes the pipeline (potentially rehydrating from `pipeline_state_in_json`),
parses `input_data_json` and `context_json`, applies any `directive_str`,
runs the pipeline's main execution logic, and then returns the run summary
and the new pipeline state, both as JSON strings.
Manages JSON decoding and error handling for inputs.
```

---

## Bando Reality Mesh Monolith (VM)

-   **Node Name (Key)**: `RealityMeshMonolithNode`
-   **File**: `victor_reality_mesh_monolith.py`
-   **Version**: `v5.0.0-REALITY-MESH-GODCORE-VM`
-   **Category**: `Victor/AGI/Mesh`
-   **Hash**: `(dynamic_hash_would_be_here)`
-   **Description (from metadata)**: 3D Fractal Flower-of-Life Reality Mesh Monolith by Bando. AGI Core Block Mesh Engine. Inject signal, propagate in 3D geometry, mutate brain.

### Class Docstring:
```
VictorModule: Wraps BandoRealityMeshMonolith as a pluggable node for Comfy Cortex.
Enables 3D mesh-based neural propagation, introspection, and real-time mutation.
```

### Inputs:
    *   **Required Inputs:**
        *   `input_signal` (`*`): Default = `N/A`. NumPy array signal. Can be from another VictorModule or a converter.
        *   `start_node_id` (`STRING`): Default = `random`. ID of mesh node or 'random'
        *   `propagation_steps` (`INT`): Default = `3`. Number of propagation steps
        *   `mesh_dim` (`INT`): Default = `64`. Dimension of mesh vectors. Re-initializes mesh if changed.
        *   `mesh_depth` (`INT`): Default = `2`. Fractal depth of the mesh. Re-initializes mesh if changed.

### Outputs:
    *   **Outputs:**
        *   `mesh_embedding_obj` (`*`)
        *   `summary_json` (`STRING`)
        *   `mesh_history_json` (`STRING`)

### Main Function (`run_monolith_node`):
```
Initializes or reuses a BandoRealityMeshMonolith instance based on `mesh_dim` and `mesh_depth`.
Processes the `input_signal` (expecting np.ndarray).
Selects a `start_node_id` (handling 'random' keyword).
Propagates the signal through the mesh using `monolith_instance.propagate()`.
Returns the final mesh embedding (as a Python object/NumPy array),
a JSON string of the monolith's summary, and a JSON string of the propagation history.
Includes error handling for input parsing, monolith initialization, and propagation.
```

---

## Directive Injector (Victor)

-   **Node Name (Key)**: `DirectiveInjectorVictorNode`
-   **File**: `victor_directive_injector.py`
-   **Version**: `v1.0.0-DIRECTIVE-INJECTOR-NODE`
-   **Category**: `VictorModules/Pipeline Control`
-   **Hash**: `(dynamic_hash_would_be_here)`
-   **Description (from metadata)**: Injects a new directive into a BandoCognitionPipeline's state (provided as JSON). Updates and outputs the new pipeline state JSON and the resulting cognitive mode.

### Class Docstring:
```
VictorModule for injecting directives into a serialized BandoCognitionPipeline state.
This node takes the JSON state output from BandoCognitionPipelineNode,
a new directive string, and optional context. It then simulates the
directive routing logic to update the mode and directive log within the state,
outputting the modified state and the new mode.
```

### Inputs:
    *   **Required Inputs:**
        *   `pipeline_state_in_json` (`STRING`): Default = `N/A`. JSON state from BandoCognitionPipelineNode.
        *   `directive_to_inject` (`STRING`): Default = `reflect`.
    *   **Optional Inputs:**
        *   `injection_context_json` (`STRING`): Default = `{}`.
        *   `available_modes_override_json` (`STRING`): Default = `["reflect", "defend", "repair", "expand", "dream"]`. JSON list of available modes for the router.

### Outputs:
    *   **Outputs:**
        *   `pipeline_state_out_json` (`STRING`)
        *   `new_mode_set_str` (`STRING`)

### Main Function (`inject_directive`):
```
Processes an incoming pipeline state (JSON), a new directive, and optional context.
It updates the 'current_directive_mode' and 'directive_log' fields within the
JSON state to reflect the injection of the new directive.
The available modes for the router can be overridden via input.
Returns the modified pipeline state as JSON and the new mode that was set.
```

---

## Echo Victor (Debug)

-   **Node Name (Key)**: `EchoVictorNode`
-   **File**: `echo_victor.py`
-   **Version**: `v1.0.0-ECHO-GODCORE`
-   **Category**: `VictorModules/Debug`
-   **Hash**: `(dynamic_hash_would_be_here)`
-   **Description (from metadata)**: Echoes the input_data, optionally prepended with a prefix. For Victor pipeline debugging.

### Class Docstring:
```
No docstring available.
```

### Inputs:
    *   **Required Inputs:**
        *   `input_data` (`STRING`): Default = `Hello Victor`.
    *   **Optional Inputs:**
        *   `prefix` (`STRING`): Default = ``.

### Outputs:
    *   **Outputs:**
        *   `output_echo` (`STRING`)

### Main Function (`forward`):
```
The main execution method called by ComfyUI.
Matches the FUNCTION specified above.
Input arguments must match keys in INPUT_TYPES.
```

---

## Load Reality Mesh State (VM)

-   **Node Name (Key)**: `MeshStateLoaderNode`
-   **File**: `victor_mesh_state_loader.py`
-   **Version**: `v5.0.0-REALITY-MESH-GODCORE-LOADER-VM`
-   **Category**: `Victor/AGI/Mesh/State`
-   **Hash**: `(dynamic_hash_would_be_here)`
-   **Description (from metadata)**: Loads a previously saved Reality Mesh state (summary and history) from a JSON file on disk. Outputs JSON strings suitable for other mesh nodes.

### Class Docstring:
```
VictorModule: Loads mesh summary and history from a previously saved JSON state file.
Outputs these as JSON strings, ready to feed back into other mesh nodes.
```

### Inputs:
    *   **Required Inputs:**
        *   `filepath` (`STRING`): Default = `saved_mesh_states/your_mesh_file.json`. Full or relative path to the saved mesh state JSON file.

### Outputs:
    *   **Outputs:**
        *   `mesh_summary_json` (`STRING`)
        *   `mesh_history_json` (`STRING`)
        *   `loaded_filepath_str` (`STRING`)

### Main Function (`load_mesh_state`):
```
Takes a filepath string to a JSON file previously saved by MeshStateSaverNode.
Reads the file, parses the JSON, and extracts the 'mesh_summary' and 'mesh_history'
components from the saved structure.
Outputs these components as individual JSON strings, along with the filepath from which data was loaded.
Handles file I/O errors and JSON parsing errors gracefully by returning error messages
and empty JSON strings for data outputs.
```

---

## Math Operation (Victor)

-   **Node Name (Key)**: `MathVictorNode`
-   **File**: `math_victor.py`
-   **Version**: `v1.0.0-MATH-CORTEX`
-   **Category**: `VictorModules/Logic`
-   **Hash**: `(dynamic_hash_would_be_here)`
-   **Description (from metadata)**: Performs various mathematical operations like add, subtract, multiply, divide, power, log, sqrt. Outputs result and status.

### Class Docstring:
```
No docstring available.
```

### Inputs:
    *   **Required Inputs:**
        *   `operation` (`['add', 'subtract', 'multiply', 'divide', 'power', 'log', 'sqrt']`): Default = `add`.
        *   `a` (`FLOAT`): Default = `0.0`.
    *   **Optional Inputs:**
        *   `b` (`FLOAT`): Default = `1.0`.
        *   `log_base` (`FLOAT`): Default = `10.0`.

### Outputs:
    *   **Outputs:**
        *   `result` (`FLOAT`)
        *   `status_message` (`STRING`)

### Main Function (`perform_operation`):
```
Performs the specified mathematical operation on input 'a' (and 'b' if applicable).
Handles division by zero, log domain errors, and sqrt domain errors by returning float('nan')
and an error message in status_message.
```

---

## MetaLoop Info (Victor)

-   **Node Name (Key)**: `MetaLoopTrendVictorNode`
-   **File**: `victor_meta_loop_trend.py`
-   **Version**: `v1.0.0-METALOOP-TREND-NODE`
-   **Category**: `VictorModules/Pipeline Inspectors`
-   **Hash**: `(dynamic_hash_would_be_here)`
-   **Description (from metadata)**: Views MetaLoop data (trends, scores, distillation steps) from a BandoCognitionPipeline's JSON state. Outputs a summary string, the last evaluation score, and total evaluation count.

### Class Docstring:
```
No docstring available.
```

### Inputs:
    *   **Required Inputs:**
        *   `pipeline_state_in_json` (`STRING`): Default = `N/A`. JSON state from BandoCognitionPipelineNode or compatible.
    *   **Optional Inputs:**
        *   `max_trends_to_display` (`INT`): Default = `5`.
        *   `max_scores_to_display` (`INT`): Default = `5`.

### Outputs:
    *   **Outputs:**
        *   `metaloop_summary_str` (`STRING`)
        *   `last_score_float` (`FLOAT`)
        *   `total_evaluations_int` (`INT`)

### Main Function (`view_metaloop_info`):
```
Parses the pipeline_state_in_json to extract the 'meta_loop_summary'.
Formats a string detailing total trends, evaluations, distillation steps,
and a list of recent evaluation scores (with their associated metadata summarized).
Outputs this summary string, the numerical value of the very last score,
and the total count of evaluations.
Note: The current pipeline state serialization only includes a summary of recent scores,
not the full trend log.
```

---

## Reality Mesh History Viewer (VM)

-   **Node Name (Key)**: `MeshHistoryViewerNode`
-   **File**: `victor_mesh_history_viewer.py`
-   **Version**: `v1.0.0-MESH-HISTORY-VM`
-   **Category**: `Victor/AGI/Mesh/Inspectors`
-   **Hash**: `(dynamic_hash_would_be_here)`
-   **Description (from metadata)**: Analyzes and displays mesh history JSON from BandoRealityMeshMonolith. Provides summary stats, step samples, or activation trajectories for specific nodes.

### Class Docstring:
```
No docstring available.
```

### Inputs:
    *   **Required Inputs:**
        *   `mesh_history_json` (`STRING`): Default = `[]`. JSON string containing the mesh history from RealityMeshMonolithNode.
    *   **Optional Inputs:**
        *   `max_steps_to_analyze` (`INT`): Default = `5`. Max number of history steps to analyze/display from the end of the log.
        *   `nodes_to_sample_per_step` (`INT`): Default = `3`. Max number of nodes to sample per step for detailed view. 0 for no node sampling.
        *   `analysis_mode` (`['summary_stats', 'full_step_sample', 'activation_trajectory']`): Default = `summary_stats`.
        *   `trajectory_node_id` (`STRING`): Default = ``. Specific node ID to track activation trajectory (for 'activation_trajectory' mode).

### Outputs:
    *   **Outputs:**
        *   `history_analysis_str` (`STRING`)

### Main Function (`display_history`):
```
Parses the mesh_history_json (a list of step snapshots, where each step is a dict of node_id to state_list).
Based on the analysis_mode, it formats a string output:
- 'summary_stats': Calculates and shows mean/min/max/stddev of activations for recent steps.
- 'full_step_sample': Shows states of a sample of nodes for recent steps.
- 'activation_trajectory': Shows mean activation of a specific node ID across history.
Handles empty or malformed history JSON.
```

---

## Reality Mesh Summary Viewer (VM)

-   **Node Name (Key)**: `MeshSummaryViewerNode`
-   **File**: `victor_mesh_summary_viewer.py`
-   **Version**: `v1.0.1-MESH-SUMMARY-VM`
-   **Category**: `Victor/AGI/Mesh/Inspectors`
-   **Hash**: `(dynamic_hash_would_be_here)`
-   **Description (from metadata)**: Takes a mesh summary JSON string (from BandoRealityMeshMonolith) and displays it in a more readable, pretty-printed format, with options for truncation.

### Class Docstring:
```
No docstring available.
```

### Inputs:
    *   **Required Inputs:**
        *   `summary_json` (`STRING`): Default = `{}`. JSON string containing the mesh summary from RealityMeshMonolithNode.
    *   **Optional Inputs:**
        *   `indent_level` (`INT`): Default = `2`. Indentation level for JSON pretty printing. 0 for compact.
        *   `max_block_assignments_preview` (`INT`): Default = `5`. Max items for 'block_assignments' preview if too large. 0 for all.

### Outputs:
    *   **Outputs:**
        *   `formatted_summary_str` (`STRING`)

### Main Function (`display_summary`):
```
Parses the summary_json string into a Python dictionary.
Constructs a human-readable summary string highlighting key metrics like
dimension, mesh depth, node/connection counts, block type counts,
mean activation, and history length.
Also includes a pretty-printed version of the full JSON (with potential
truncation of very large 'block_assignments' dictionary for brevity).
Handles JSON decoding errors.
```

---

## Save Reality Mesh State (VM)

-   **Node Name (Key)**: `MeshStateSaverNode`
-   **File**: `victor_mesh_state_saver.py`
-   **Version**: `v5.0.0-REALITY-MESH-GODCORE-SAVER-VM`
-   **Category**: `Victor/AGI/Mesh/State`
-   **Hash**: `(dynamic_hash_would_be_here)`
-   **Description (from metadata)**: Saves the current Reality Mesh state (summary and history JSONs) to a timestamped JSON file on disk for persistence and later reloading.

### Class Docstring:
```
VictorModule: Dumps the mesh state (summary + history from JSON inputs) to a .json file.
```

### Inputs:
    *   **Required Inputs:**
        *   `mesh_summary_json` (`STRING`): Default = `N/A`. JSON string of the mesh summary.
        *   `mesh_history_json` (`STRING`): Default = `N/A`. JSON string of the mesh history.
    *   **Optional Inputs:**
        *   `output_directory` (`STRING`): Default = `saved_mesh_states`. Directory to save mesh state files.
        *   `filename_tag` (`STRING`): Default = `snapshot`. Custom tag for the output filename.

### Outputs:
    *   **Outputs:**
(This node is an OUTPUT_NODE, typically no data outputs for further processing in graph)

### Main Function (`save_mesh_state`):
```
Takes JSON strings for mesh_summary and mesh_history.
Constructs a state dictionary including these, versioning info, and timestamps.
Saves this dictionary as a JSON file to the specified `output_directory`
with a filename formatted as `mesh_state_[filename_tag]_[timestamp].json`.
Handles directory creation and file I/O errors.
Returns a dictionary for potential UI updates (e.g., showing the saved filepath).
```

---

## Telemetry Viewer (Victor)

-   **Node Name (Key)**: `TelemetryViewerVictorNode`
-   **File**: `victor_telemetry_viewer.py`
-   **Version**: `v1.0.0-TELEMETRY-VIEWER-NODE`
-   **Category**: `VictorModules/Pipeline Inspectors`
-   **Hash**: `(dynamic_hash_would_be_here)`
-   **Description (from metadata)**: Views telemetry data from a BandoCognitionPipeline's JSON state. Can output a summary, a sample of the full log, or logs for a specific signal type.

### Class Docstring:
```
No docstring available.
```

### Inputs:
    *   **Required Inputs:**
        *   `pipeline_state_in_json` (`STRING`): Default = `N/A`. JSON state from BandoCognitionPipelineNode or compatible.
    *   **Optional Inputs:**
        *   `signal_type_filter` (`STRING`): Default = ``. Optional: Filter telemetry by this signal type.
        *   `last_n_entries` (`INT`): Default = `5`. Number of recent entries to display per signal type or overall.
        *   `output_format` (`['summary', 'full_log_sample', 'signal_specific_log']`): Default = `summary`.

### Outputs:
    *   **Outputs:**
        *   `telemetry_output_str` (`STRING`)

### Main Function (`view_telemetry`):
```
Parses the pipeline_state_in_json to extract the 'pulse_telemetry_summary'.
Based on the output_format, it generates a string:
- 'summary': Shows total pulses, observed signal types, and the last recorded signal.
- 'full_log_sample': Shows a sample of recent pulses from the available log in the state.
- 'signal_specific_log': Shows recent pulses filtered by the 'signal_type_filter'.
Handles cases where state or specific telemetry data is missing.
```

---

## Tokenizer (Victor)

-   **Node Name (Key)**: `TokenizerVictorNode`
-   **File**: `tokenizer_victor.py`
-   **Version**: `v1.0.0-TOKENIZER-CORTEX`
-   **Category**: `VictorModules/Text Processing`
-   **Hash**: `(dynamic_hash_would_be_here)`
-   **Description (from metadata)**: Splits input text into a list of tokens based on a delimiter. Supports whitespace, comma, or custom regex delimiters and optional lowercasing.

### Class Docstring:
```
No docstring available.
```

### Inputs:
    *   **Required Inputs:**
        *   `text` (`STRING`): Default = `Hello world, this is Comfy Cortex!`.
        *   `delimiter_type` (`['whitespace', 'comma', 'custom_regex']`): Default = `whitespace`.
    *   **Optional Inputs:**
        *   `custom_delimiter_regex` (`STRING`): Default = `\s+`.
        *   `to_lowercase` (`BOOLEAN`): Default = `False`.

### Outputs:
    *   **Outputs:**
        *   `token_list_obj` (`*`)

### Main Function (`tokenize_text`):
```
Tokenizes the input text based on the specified delimiter.
Outputs a list of string tokens.
```

---

...(Conceptual entries for Fractal and Audio nodes would also be here if they were fully implemented)...

---
