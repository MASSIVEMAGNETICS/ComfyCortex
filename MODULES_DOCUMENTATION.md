# VictorModules Documentation

This document provides details for all loaded VictorModules in Comfy Cortex.
It is auto-generated by `generate_docs.py`.

---

## Bando Cognition Pipeline Core

-   **Node Name (Key)**: `BandoCognitionPipelineNode`
-   **File**: `victor_bando_cognition_pipeline.py`
-   **Version**: `v1.0.1-GODCORE-FUSION-NODE-REFACTORED`
-   **Category**: `VictorModules/Pipelines`
-   **Hash**: `(dynamic_hash_would_be_here)`
-   **Description (from metadata)**: Core node for running the Bando AGI Cognition Pipeline. Manages focus, directives, telemetry, and meta-loop processes. Uses imported core logic from 'bando_pipeline_core'.

### Class Docstring:
```
VictorModule Wrapper for BandoCognitionPipelineCore.
This node instantiates and runs the main cognitive pipeline.
It takes JSON strings for input data, context, and initial/rehydration state.
Outputs include a summary of the run and the updated pipeline state as JSON.
```

### Inputs:
    *   **Required Inputs:**
        *   `input_data_json` (`STRING`): Default = `{"prompt": "Hello Bando!"}`. Main input data for the pipeline (JSON string).
    *   **Optional Inputs:**
        *   `context_json` (`STRING`): Default = `{}`. Contextual information for the pipeline run (JSON string).
        *   `directive_str` (`STRING`): Default = ``. Initial directive (e.g., 'expand', 'reflect'). Empty for default.
        *   `pipeline_state_in_json` (`STRING`): Default = ``. JSON state from a previous run to rehydrate the pipeline.
        *   `initial_modes_json_list` (`STRING`): Default = `["reflect", "defend", "repair", "expand", "dream"]`. JSON list of modes for the directive router.

### Outputs:
    *   **Outputs:**
        *   `output_summary_json` (`STRING`)
        *   `pipeline_state_out_json` (`STRING`)

### Main Function (`execute_pipeline`):
```
Handles the execution of the BandoCognitionPipelineCore.
It initializes the pipeline (potentially rehydrating from `pipeline_state_in_json`),
parses `input_data_json` and `context_json`, applies any `directive_str`,
runs the pipeline's main execution logic, and then returns the run summary
and the new pipeline state, both as JSON strings.
Manages JSON decoding and error handling for inputs.
```

---

## Directive Injector (Victor)

-   **Node Name (Key)**: `DirectiveInjectorVictorNode`
-   **File**: `victor_directive_injector.py`
-   **Version**: `v1.0.0-DIRECTIVE-INJECTOR-NODE`
-   **Category**: `VictorModules/Pipeline Control`
-   **Hash**: `(dynamic_hash_would_be_here)`
-   **Description (from metadata)**: Injects a new directive into a BandoCognitionPipeline's state (provided as JSON). Updates and outputs the new pipeline state JSON and the resulting cognitive mode.

### Class Docstring:
```
VictorModule for injecting directives into a serialized BandoCognitionPipeline state.
This node takes the JSON state output from BandoCognitionPipelineNode,
a new directive string, and optional context. It then simulates the
directive routing logic to update the mode and directive log within the state,
outputting the modified state and the new mode.
```

### Inputs:
    *   **Required Inputs:**
        *   `pipeline_state_in_json` (`STRING`): Default = `N/A`. JSON state from BandoCognitionPipelineNode.
        *   `directive_to_inject` (`STRING`): Default = `reflect`.
    *   **Optional Inputs:**
        *   `injection_context_json` (`STRING`): Default = `{}`.
        *   `available_modes_override_json` (`STRING`): Default = `["reflect", "defend", "repair", "expand", "dream"]`. JSON list of available modes for the router.

### Outputs:
    *   **Outputs:**
        *   `pipeline_state_out_json` (`STRING`)
        *   `new_mode_set_str` (`STRING`)

### Main Function (`inject_directive`):
```
Processes an incoming pipeline state (JSON), a new directive, and optional context.
It updates the 'current_directive_mode' and 'directive_log' fields within the
JSON state to reflect the injection of the new directive.
The available modes for the router can be overridden via input.
Returns the modified pipeline state as JSON and the new mode that was set.
```

---

## Echo Victor (Debug)

-   **Node Name (Key)**: `EchoVictorNode`
-   **File**: `echo_victor.py`
-   **Version**: `v1.0.0-ECHO-GODCORE`
-   **Category**: `VictorModules/Debug`
-   **Hash**: `(dynamic_hash_would_be_here)`
-   **Description (from metadata)**: Echoes the input_data, optionally prepended with a prefix. For Victor pipeline debugging.

### Class Docstring:
```
No docstring available.
```

### Inputs:
    *   **Required Inputs:**
        *   `input_data` (`STRING`): Default = `Hello Victor`.
    *   **Optional Inputs:**
        *   `prefix` (`STRING`): Default = ``.

### Outputs:
    *   **Outputs:**
        *   `output_echo` (`STRING`)

### Main Function (`forward`):
```
The main execution method called by ComfyUI.
Matches the FUNCTION specified above.
Input arguments must match keys in INPUT_TYPES.
```

---

...(Other modules like Tokenizer, Math, TelemetryViewer, MetaLoopTrendNode would follow the same format)...

---
